import 'package:flutter/material.dart';

class MyColor {
  static const author = Color(0x8A000000);
  static const headingTitle = Color(0xff000000);
  static const bgNewsCard = Colors.black45;
  static const titleCard = Colors.white;
  static const authorCard = Colors.white70;
  static const iconColors = Color(0xff000000);
  static const menuAppbar = Color(0xff000000);
  static const colorNavbar = Color(0xff64B5F6);
  static const btnNavbar = Color(0xffEF5350);
  static const bgNavbar = Colors.transparent;
}
